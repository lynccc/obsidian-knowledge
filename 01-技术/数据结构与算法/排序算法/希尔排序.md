---
tags: [数据结构, 算法, 排序, 希尔排序]
aliases: [Shell Sort, ShellSort, 递减增量排序, 缩小增量排序]
created: 2026-06-25
---

# 希尔排序

## 🗣️ 通俗讲解
> 插入排序对几乎排好序的数据很快，希尔排序就利用了这一点：先隔几个数排一次（大间隔预排序），再缩小间隔继续排，最后间隔为 1 时数据已基本有序，插入排序就飞快了。就像先把大件行李归位，再整理小物件。

## 📖 核心概念

希尔排序是**插入排序的改进版**，通过**递减增量（diminishing increment）**策略大幅提升效率。

基本思想：先以较大增量对序列做间隔插入排序，逐步缩小增量，最终增量为 1 时退化为普通插入排序。但经过前面各趟的"预排序"，序列已接近有序，最后一趟效率极高。

### 增量序列的选择

| 增量序列 | 时间复杂度 |
|----------|-----------|
| Shell (n/2, n/4, ...) | O(n²) |
| Knuth (1, 4, 13, 40, ...) | O(n^(3/2)) |
| Sedgewick (1, 5, 19, 41, ...) | O(n^(4/3)) 经验值 |

### 复杂度

- 空间复杂度：**O(1)**（原地排序）
- **不稳定排序**

### 关键特点
- 是第一个突破 O(n²) 的排序算法（历史上）
- 实现简单，不需要额外空间
- 对中等规模数据集是个好选择

## 📝 各书特色

### 邓俊辉版（C++）
- 侧重：增量序列设计的理论分析，Knuth 序列和 Sedgewick 序列的推导
- 特色：强调递减增量的渐进策略思想

### Miller版（Python）
- 侧重：Python 实现的 `gap_insertion_sort` 辅助函数，Knuth 序列的生成
- 特色：过程示例清晰展示各趟排序效果

## 🔗 相关笔记
- [[插入排序]] — 希尔排序的基础
- [[归并排序]] — 归并排序有更好的理论复杂度
- [[排序算法总结]] — 所有排序算法对比

## 📚 参考
- 《数据结构（C++语言版）》邓俊辉 §12.3
- 《Python数据结构与算法分析》Miller §5.3.4
