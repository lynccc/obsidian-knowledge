---
tags: [数据结构, C++, 邓俊辉, AVL, 平衡树]
aliases: [AVL Tree, Adelson-Velsky-Landis Tree]
---

# AVL树

## 核心概念

AVL树是最早被发明的[[平衡二叉搜索树]]，由 Adelson-Velsky 和 Landis 于 1962 年提出。其核心约束：**任意节点的平衡因子绝对值不超过 1**，即 `|bf(v)| ≤ 1`。这一简单约束保证了树高为 O(log n)。

## 关键要点

### 定义与性质

- **AVL 条件**：对每个节点 v，`bf(v) ∈ {-1, 0, 1}`
- 含 n 个节点的 AVL 树高度 h ≤ 1.44 log₂(n+2) ≈ **O(log n)**
- 比理想平衡略差，但维护成本更低

### 节点插入

插入新节点后，可能破坏祖先节点的 AVL 条件：

1. **失衡检测**：从插入点向上回溯，找到第一个失衡节点 g
2. **失衡原因**：g 的某个孩子的子树因插入而"长高"
3. **重平衡**：通过旋转恢复平衡，分四种情况：

| 情况 | 形态 | 旋转方式 |
|------|------|----------|
| LL | 在 g 的左孩子的左子树插入 | 对 g 做**右旋** |
| RR | 在 g 的右孩子的右子树插入 | 对 g 做**左旋** |
| LR | 在 g 的左孩子的右子树插入 | 先对左孩子**左旋**，再对 g **右旋** |
| RL | 在 g 的右孩子的左子树插入 | 先对右孩子**右旋**，再对 g **左旋** |

> 插入后至多需要 **1 次**单旋或双旋即可恢复全局平衡。

### 节点删除

删除比插入更复杂：

1. 执行标准 BST 删除
2. 从删除点向上回溯，检查每个祖先的平衡因子
3. 失衡时进行旋转修复
4. 旋转后**可能引发上层继续失衡**，需继续向上检查

> 删除后可能需要 **O(log n) 次**旋转（而非插入时的 O(1) 次）。

### 统一重平衡算法

针对 4 种失衡情况的统一处理思路：

1. 确定**旋转轴**（3 个节点中高度居中者）
2. 对这 3 个节点做重平衡（单旋或双旋）
3. 重新连接子树（最多 4 棵子树需要重新挂载）
4. 更新受影响节点的高度

这种统一视角将 LL/RR/LR/RL 四种情况归纳为同一套流程。

## 复杂度

| 操作 | 时间复杂度 |
|------|-----------|
| 查找 | O(log n) |
| 插入 | O(log n)，最多 1 次旋转 |
| 删除 | O(log n)，最多 O(log n) 次旋转 |

## 与其他概念的联系

- 基于 [[平衡二叉搜索树]] 的旋转机制
- 是 [[二叉搜索树BST]] 的平衡化实现
- 后续改进：[[红黑树]]（更少的旋转次数）、[[B树]]（多路平衡）
- 插入的 LL/RR/LR/RL 分类是理解所有平衡树的基础

## 🆚 C++ vs Python 实现对比

> 💡 **小白提示：** 这个概念在两本书里都有讲，但用的语言不同。
> - **C++ 版**（邓俊辉）：更偏理论，讲底层原理，适合考研/面试深入理解
> - **Python 版**（Miller）：更偏实践，代码简洁易读，适合快速上手
>
> 📖 Python 版笔记：[[AVL树Python]]

## 参考
- 《数据结构（C++语言版）》第3版 邓俊辉 §7.4
