---
aliases: [import, 导入模块, 代码组织]
tags: [技术, Python, 基础, 模块]
created: 2026-06-25 01:48
source: Python编程：从入门到实践
related: [[函数基础]], [[类基础]]
---

# 📦 Python 模块

## 核心概念

- 模块是包含函数的 `.py` 文件
- `import` 导入整个模块
- `from...import` 导入特定函数
- `as` 给函数/模块指定别名

## 导入整个模块

```python
# pizza.py
def make_pizza(size, *toppings):
    print("\nMaking a " + str(size) + "-inch pizza with the following toppings:")
    for topping in toppings:
        print("- " + topping)

# making_pizzas.py
import pizza
pizza.make_pizza(16, 'pepperoni')
pizza.make_pizza(12, 'mushrooms', 'green peppers', 'extra cheese')
```

## 导入特定函数

```python
from pizza import make_pizza
make_pizza(16, 'pepperoni')  # 不需要 pizza. 前缀
```

## 使用别名

```python
# 函数别名
from pizza import make_pizza as mp
mp(16, 'pepperoni')

# 模块别名
import pizza as p
p.make_pizza(16, 'pepperoni')

# 导入所有函数（不推荐）
from pizza import *
make_pizza(16, 'pepperoni')
```

## 相关笔记

- [[函数基础]] — 定义函数
- [[类基础]] — 定义类

---

*由奶茶一号整理 🧋*
